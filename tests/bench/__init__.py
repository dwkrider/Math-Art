# Solver benchmark harness (headless, deterministic, numpy-only).
#
# Measures speed and quality of the iterative solvers in math_art/ on
# fixed test cases, so solver changes are accepted or rejected on
# numbers rather than eyeballing.  See tests/bench/run.py for the CLI.
